# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native (CNG — regenerated by prebuild; never hand-edit, never commit)
/ios
/android
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files — per-machine tokens, never committed
.env*.local

# typescript
*.tsbuildinfo

# isolated concurrent streams — one branch, one worktree, one window
.worktrees/

# SDD ledger and brainstorm mockups — scratch, not content
.superpowers/
