/**
 * Shared geometry primitives for the chart wheel.
 *
 * Mirrors: kairos-ios ChartGeometry.swift's use of CoreGraphics `CGPoint`
 * (the TS port carries plain `{ x, y }` objects instead).
 */
export type Point = { x: number; y: number };
